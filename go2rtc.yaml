# https://github.com/AlexxIT/go2rtc#configuration


hass:
  config: "/config"  # skip this setting if you Hass Add-on user

streams:
#  generic_camera: hass:Camera1  # Settings > Integrations > Integration Name
  corridor: hass:Camera-Hub-G3-0BA5

  dahua_camera:
    - rtsp://admin:adhome619@192.168.192.55/cam/realmonitor?channel=1&subtype=0&unicast=true&proto=Onvif
    - rtsp://admin:adhome619@192.168.33.230/cam/realmonitor?channel=1&subtype=0&unicast=true&proto=Onvif

webrtc:
  listen: ":8555"  # address of your local server (TCP)
  candidates:
    - 95.31.1.7:8555  # if you have static public IP-address
log:
  level: debug